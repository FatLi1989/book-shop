spring:
  cloud:
    nacos:
      config:
        server-addr: 47.56.112.40:8848  # nacos 服务地址
        file-extension: yaml            # 扩展名
        ext-config:                     # 多配置文件
         - data-id: content-center-dev.yaml
           group: DEFAULT_GROUP
           refresh: true
#  application:
#    name: content-center
#  profiles:
#    active: dev
